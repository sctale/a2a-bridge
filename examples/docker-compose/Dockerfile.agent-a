# Agent A — Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY ../requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码（上层目录）
COPY ../server /app/server
WORKDIR /app

# 默认启动命令
CMD ["python", "server/server_side.py"]
